<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.lcy.yozoraforum.mapper.ForumTagRelationMapper">
    <insert id="insert">
        insert into forum_tag_relation (tag_id,forum_id)
        values
        <foreach collection="tagsList" item="tagId" separator=",">
            (#{tagId},#{forumId})
        </foreach>
    </insert>

    <select id="selectTags" resultType="com.lcy.yozoraforum.entity.Tags">
        select t.tag_id, t.tag_name
        from forum_tag_relation f
                 left join tags t on t.tag_id = f.tag_id
        where f.forum_id = #{forumId};
    </select>

    <select id="selectForumByTags" resultType="com.lcy.yozoraforum.wrapper.ForumWrapper">
        select f.*,count(c.comment_id) AS forum_comment_count, u.user_name from forum_tag_relation ftr
        left join forum f  on ftr.forum_id = f.forum_id
        left join comments c on ftr.forum_id  = c.forum_id
        left join user u on f.user_id = u.user_id
        where ftr.tag_id in
        <foreach collection="tagIds" item="tagId" separator="," open="(" close=")">
            #{tagId}
        </foreach>
        GROUP BY f.forum_id
    </select>
</mapper>