<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.lcy.yozoraforum.mapper.CommentsLikeUserRelationMapper">
    <insert id="insertRelation">
        insert ignore into comments_like_user_relation (comment_id,user_id,status,update_time) values
        <foreach collection="relationList" item="uid" separator=",">
            (#{uid.commentId},#{uid.userId},#{uid.status},#{uid.updateTime})
        </foreach>
        on duplicate key update
        status = VALUES(status),
        update_time = VALUES(update_time);

    </insert>
</mapper>