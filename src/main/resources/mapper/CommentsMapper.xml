<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.lcy.yozoraforum.mapper.CommentsMapper">
    <insert id="insert">
        insert into comments
        <trim prefix="(" suffix=")" suffixOverrides=",">
            user_id,
            forum_id,
            resource_id,
            parent_id,
            create_time,
            comment_like,
            <if test="insertCommentsDTO.content != null and insertCommentsDTO.content != ''">content</if>
        </trim>
        values
        <trim prefix="(" suffix=")" suffixOverrides=",">
            #{userId},
            #{insertCommentsDTO.forumId},
            #{insertCommentsDTO.resourceId},
            #{insertCommentsDTO.parentId},
            now(),
            0
            <if test="insertCommentsDTO.content != null and insertCommentsDTO.content != ''">, #{insertCommentsDTO.content}</if>
        </trim>
    </insert>

    <select id="getForumAllComments" resultType="com.lcy.yozoraforum.vo.CommentsVO">
        select c.*,u.user_name from comments c left join user u on c.user_id = u.user_id where forum_id = #{forumId} and sender_user_id is null and parent_id = 0 order by create_time desc limit #{offset},#{pageSize}
    </select>


    <select id="selectPosition" resultType="java.lang.Long">
        SELECT COUNT(*)
        FROM comments
        WHERE
        forum_id = #{forumId} AND (create_time >= #{createTime} and parent_id = #{parentId});
    </select>

    <select id="selectAll" resultType="java.lang.Long">
        select count(*) from comments where forum_id = #{forumId}
    </select>

    <select id="selectNoChildCommentCount" resultType="java.lang.Long">
        select count(*) from comments where forum_id = #{forumId} and parent_id = 0
    </select>

    <select id="getChildComments" resultType="com.lcy.yozoraforum.vo.CommentsVO">
        select c.*,u.user_name from comments c left join user u on c.user_id = u.user_id where
        <foreach item="id" index="index" collection="commentsVOList" open="(" separator="or" close=")">
            parent_id = #{id.commentId}
        </foreach>
    </select>

</mapper>